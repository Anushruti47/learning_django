from django.db import models

# Create your models here.
# class Product(models.Model):
#     product_name=models.CharField(max_length=100)
#     rating=models.ChoiceField(choices=[(str(i), str(i)) for i in range(1,6)])
#     feedback_text=models.Textarea(attrs={'rows':50,'cols':200})
#     reason_for_low_rating=models.CharField(required=False)

#     def __str__(self):
#         return self.product_name

